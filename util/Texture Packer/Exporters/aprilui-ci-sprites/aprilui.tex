<?xml version="1.0"?>
<Textures>

	<Texture filename="{{texture.trimmedName}}" dynamic_load="1">{% for sprite in allSprites %}
		<Image name="{{sprite.trimmedName}}" x="{{sprite.frameRect.x}}" y="{{sprite.frameRect.y}}" w="{{sprite.frameRect.width}}" h="{{sprite.frameRect.height}}"{% if sprite.rotated %} vertical="1"{% endif %}/>{% endfor %}
	</Texture>
	
</Textures>
